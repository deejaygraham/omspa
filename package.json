{
  "name": "strpat-handbook-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy --config .eleventy.cjs",
    "start": "eleventy --config .eleventy.cjs --serve",
    "convert": "node scripts/convert-html-to-md.js",
    "test": "npm run build && playwright test",
    "test:watch": "playwright test --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@playwright/test": "^1.60.0",
    "fs-extra": "^11.1.1",
    "glob": "^10.4.1",
    "jsdom": "^22.1.0",
    "markdown-it": "^13.0.1",
    "turndown": "^7.1.1"
  }
}
